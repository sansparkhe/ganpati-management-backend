# ---- Core framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ---- Database ----
sqlalchemy[asyncio]==2.0.36
psycopg[binary,pool]==3.2.3        # PostgreSQL driver (works for BOTH sync + async)
alembic==1.14.0
greenlet==3.1.1                    # required by SQLAlchemy asyncio

# ---- Validation / settings ----
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# ---- Testing ----
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
aiosqlite==0.20.0                  # test-only DB driver, so tests need no PostgreSQL
